# API inventory: BUILD_SPEC §8 vs implemented

Generated by reading every `apps/web/app/api/**/route.ts` and
`services/analytics/app/api/internal.py`. "Extra" means implemented but
not in the §8 tables; "missing" means specified but with no route file.

## Web `/api/v1/*` — public/guest (§8.1)

| Method | Route              | Status      |
| ------ | ------------------ | ----------- |
| GET    | `/players`         | implemented |
| GET    | `/players/:slug`   | implemented |
| POST   | `/players/compare` | implemented |
| GET    | `/rankings`        | implemented |
| GET    | `/data-sources`    | implemented |
| POST   | `/demo-drafts`     | implemented |

## Web — authenticated user (§8.2)

| Method           | Route                                     | Status                                                         |
| ---------------- | ----------------------------------------- | -------------------------------------------------------------- |
| GET/POST         | `/leagues`                                | implemented                                                    |
| GET/PATCH/DELETE | `/leagues/:id`                            | implemented                                                    |
| POST             | `/leagues/:id/clone`                      | implemented                                                    |
| GET              | `/leagues/:id/strategy-preview`           | **extra** (Phase 3B preview, never persists)                   |
| GET/POST         | `/preference-profiles`                    | implemented                                                    |
| GET/PATCH/DELETE | `/preference-profiles/:id`                | implemented                                                    |
| GET/PUT          | `/custom-ranks`                           | implemented                                                    |
| GET              | `/teams`                                  | **extra** (team metadata lookup for the preference editor)     |
| GET/POST         | `/drafts`                                 | implemented                                                    |
| GET              | `/drafts/:id`                             | implemented                                                    |
| POST             | `/drafts/:id/start`                       | implemented                                                    |
| POST             | `/drafts/:id/picks`                       | implemented                                                    |
| POST             | `/drafts/:id/undo`                        | implemented                                                    |
| POST             | `/drafts/:id/pause`                       | implemented                                                    |
| POST             | `/drafts/:id/resume`                      | implemented                                                    |
| POST             | `/drafts/:id/complete`                    | **extra** (armed two-step completion; mirrors demo `complete`) |
| POST             | `/drafts/:id/cpu-pick`                    | implemented (owner mock only)                                  |
| GET              | `/drafts/:id/events`                      | implemented (legacy array + cursor mode)                       |
| GET              | `/drafts/:id/recommendations`             | implemented                                                    |
| POST             | `/drafts/:id/recommendations/recalculate` | implemented (4/30 s gate)                                      |
| POST/DELETE      | `/drafts/:id/share`                       | implemented                                                    |
| GET              | `/drafts/:id/analysis`                    | implemented                                                    |
| GET              | `/me/history`                             | implemented                                                    |

Not observed: `POST /drafts/:id/analysis/recalculate` (ADR 0015 D5 names
it as a future option, "rate-limited later" — no route file exists).

## Web — guest demo lifecycle (§8.1 specifies only `POST /demo-drafts`)

| Method | Route                       | Notes                                                                  |
| ------ | --------------------------- | ---------------------------------------------------------------------- |
| POST   | `/demo-drafts`              | create; returns recovery code once + sets cookie                       |
| GET    | `/demo-drafts/:id`          | resume/read (cookie or bearer)                                         |
| POST   | `/demo-drafts/:id/picks`    | guest pick (`If-Match` + `Idempotency-Key`)                            |
| POST   | `/demo-drafts/:id/cpu-pick` | CPU advance, no player input                                           |
| POST   | `/demo-drafts/:id/undo`     | undo latest                                                            |
| POST   | `/demo-drafts/:id/complete` | armed two-step completion                                              |
| POST   | `/demo-drafts/:id/abandon`  | revoke capability, mark abandoned (no `If-Match`: one-way destructive) |

## Web — admin (§8.3)

| Method         | Route                                    | Status                                                                                                                                                           |
| -------------- | ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GET            | `/admin/model-performance`               | **SPECIFIED-BUT-MISSING** — named in BUILD_SPEC §8.3, no route file exists; the string `model-performance` appears nowhere else in the repo (verified by search) |
| GET/POST/PATCH | `/admin/projection-overrides` (+ `/:id`) | implemented                                                                                                                                                      |
| GET/POST/PATCH | `/admin/player-signals` (+ `/:id`)       | implemented                                                                                                                                                      |
| GET            | `/admin/audit-log`                       | implemented                                                                                                                                                      |

Per §8.3's prohibition, no ingestion-trigger or raw-error admin endpoint
exists (ADR 0009).

## Non-`/v1` web routes (present, outside the §8 tables)

- `GET /api/health` — smoke/health shape the envelope pattern extends
  (`apps/web/lib/api/envelope.ts:1-9`).
- `POST /api/inngest` — Inngest serve endpoint
  (`apps/web/app/api/inngest/route.ts`).
- `POST /api/v1/webhooks/clerk` — signed Svix user mirror (required by
  §13; ADR 0008).
- `GET /share/:token` — page route, not an API route; redacted public
  view (ADR 0016 R6).

## Analytics internal API (§8.4)

| Method | Route                            | Status                                                                     |
| ------ | -------------------------------- | -------------------------------------------------------------------------- |
| GET    | `/health/live`                   | implemented                                                                |
| GET    | `/health/ready`                  | implemented                                                                |
| POST   | `/internal/v1/ingestion/:source` | implemented; only `demo-file` in `SUPPORTED_INGESTION_SOURCES`, others 404 |
| POST   | `/internal/v1/projections/run`   | implemented                                                                |
| POST   | `/internal/v1/models/train`      | **deliberately absent** (deterministic baseline; Phase 4 scope)            |
| POST   | `/internal/v1/models/evaluate`   | implemented (extra vs §8.4 table; backs the evaluation report)             |
| GET    | `/internal/v1/jobs/:id`          | implemented                                                                |

## Sources

- `BUILD_SPEC.md` §8.1–§8.4
- `apps/web/app/api/**/route.ts` (inventory as observed)
- `services/analytics/app/api/internal.py`
- `docs/adr/0008-background-jobs-and-caching.md`
- `docs/adr/0015-draft-analysis-and-history.md`
- `apps/web/lib/api/envelope.ts`
