"""Pydantic mirrors of the shared TypeScript contracts in packages/domain,
validated against the same fixtures in data/schemas/fixtures — see
tests/test_contracts.py. Recommendation/projection domain logic is Phase 1+."""
